care-price-angular-node
==================

This repository contain a care-price project made with AngularJS, Express, Mongo and ms-uidev-framework.

#### To run this example: 
1. `npm install`
2. `node web-server.js`